**/bin/
**/obj/
**/TestResults/
web/node_modules/
web/dist/
.vscode/
.idea/
*.user
*.suo
artifacts/
# Anchored to the repository root: this is the runtime secret-file mount the
# README and compose.yaml create (./secrets). Unanchored, it also matched the
# src/SqlSimCity.SqlServer/Secrets source directory on case-insensitive
# filesystems and silently excluded new source files from commits.
/secrets/
*.log

# Local cache written by the impeccable design-review tooling. It records
# per-file hook state for this checkout only and carries no project meaning.
.impeccable/
