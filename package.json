{
  "name": "sqlsimcity-sql-probe-catalog",
  "version": "1.0.0",
  "private": true,
  "description": "Validation for the read-only SQL Server / Azure SQL Database probe catalog under sql/.",
  "type": "module",
  "scripts": {
    "test": "node --test test/validate-manifest.test.mjs"
  }
}
